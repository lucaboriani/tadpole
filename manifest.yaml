packages:
  addr:
    actions:
      ping:
        function:  packages/addr/ping.js
        web: true
        inputs:
          redis: "redis://redis"
      all:
        function:  packages/addr/all.js
        web: true
        inputs:
          redis: "redis://redis"
      set:
        function:  packages/addr/set.js
        web: true
        inputs:
          redis: "redis://redis"
      get:
        function:  packages/addr/get.js
        web: true
        inputs:
          redis: "redis://redis"
      del:
        function:  packages/addr/del.js
        web: true
        inputs:
          redis: "redis://redis"
      delall:
        function:  packages/addr/delall.js
        inputs:
          redis: "redis://redis"
  tasklist:
    actions:
      ping:
        function:  packages/tasklist/ping.js
        web: true
        inputs:
          redis: "redis://redis"
      all:
        function:  packages/tasklist/all.js
        web: true
        inputs:
          redis: "redis://redis"
      set:
        function:  packages/tasklist/set.js
        web: true
        inputs:
          redis: "redis://redis"
      get:
        function:  packages/tasklist/get.js
        web: true
        inputs:
          redis: "redis://redis"
      del:
        function:  packages/tasklist/del.js
        web: true
        inputs:
          redis: "redis://redis"
  hello_lang:
    actions:
      hello_go:
        function: packages/hello_lang/hello-go.go
      hello_php:
        function: packages/hello_lang/hello-php.php
      hello_python:
        function: packages/hello_lang/hello-python.py